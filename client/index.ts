import { Context } from '@koishijs/client'

export default (ctx: Context) => {
  // ctx.menu('config.tree', [{
  //   id: 'config.tree.switch-logger',
  //   icon: 'edit',
  //   label: 'SABS',
  // }])

  // ctx.slot({
  //   type: 'plugin-details',
  //   component: () => '<button>abcd</button>',
  //   order: -801,
  // })

  // ctx.internal.menus['config.tree'] = []

  // ctx.action('config.tree.switch-logger', {
  //   hidden: ({ config }) => true,
  //   action: ({ config }) => { console.log(config) },
  // })

  // console.log(ctx.internal.menus)
}
